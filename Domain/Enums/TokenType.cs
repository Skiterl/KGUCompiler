namespace Domain.Enums
{
    public enum TokenType
    {
        KEYWORD,
        TYPE,
        BINARY_OPERATOR,
        UNARY_OPERATOR,
        CONST,
        ID,
        SEPARATOR,
        ERROR,
        EOF
    }
}
